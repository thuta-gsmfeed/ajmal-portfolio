import Image from "next/image";
import { products } from "@/data/content";
import { SectionTitle } from "@/components/ui/SectionTitle";
import { ArrowUpRight } from "lucide-react";
export function ProductsSection(){return <section id="products" className="py-28 md:py-40"><div className="container"><SectionTitle kicker="Current products" title="Products built for the future." body="Digital ventures designed around utility, simplicity, and scalable impact."/><div className="mt-24 space-y-32">{products.map((p,i)=><article key={p.name} className="grid items-center gap-10 lg:grid-cols-12"><div className={`lg:col-span-8 ${i%2?"lg:col-start-5":""}`}><div data-cursor="View" className="rounded-[1.5rem] border border-white/15 bg-[#111416] p-2 shadow-2xl shadow-cyan-900/10"><div className="mb-2 flex gap-1.5 px-2 py-1"><i className="size-2 rounded-full bg-white/25"/><i className="size-2 rounded-full bg-white/15"/><i className="size-2 rounded-full bg-white/10"/></div><div className="image-wrap relative aspect-[16/10] rounded-xl"><Image src={p.image.src} alt={p.image.alt} fill sizes="(max-width:1024px) 100vw, 65vw" className="object-cover"/></div></div></div><div className={`lg:col-span-4 ${i%2?"lg:col-start-1 lg:row-start-1":""}`}><span className="font-mono text-xs text-cyan-200">0{i+1} / {p.year}</span><p className="mt-8 text-sm uppercase tracking-[.18em] text-white/40">{p.category}</p><h3 className="mt-3 text-5xl tracking-[-.055em]">{p.name}</h3><p className="mt-6 leading-relaxed text-white/50">{p.description}</p><div className="mt-7 flex flex-wrap gap-2">{p.technology.map(t=><span key={t} className="rounded-full border border-white/15 px-3 py-1 text-xs text-white/50">{t}</span>)}</div><a href={p.url} className="mt-9 inline-flex items-center gap-2 border-b border-white/40 pb-2 text-sm">Visit website <ArrowUpRight size={15}/></a></div></article>)}</div></div></section>}
